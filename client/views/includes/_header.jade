template(name="header")
  header.navbar
    .navbar-inner
      a.btn.btn-navbar(data-toggle='collapse', data-target='.nav-collapse')
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.brand(href="{{pathFor 'postsList'}}") Microscope 
      .nav-collapse.collapse
        ul.nav
          if currentUser
            li
              a(href="{{pathFor 'postSubmit'}}") Submit Post
            li.dropdown
              +notifications
        ul.nav.pull-right
          li
            +loginButtons
